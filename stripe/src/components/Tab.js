//import liraries
import React, { Component } from 'react';

// create a component
export default class Checkout extends Component {
    render() {
        return (
            this.props.children
        );
    }
}
